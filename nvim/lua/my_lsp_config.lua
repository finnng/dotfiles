-- This file is no longer needed since LSP configuration has been moved to init.lua
-- All LSP setup is now handled directly in mason-lspconfig.setup() with handlers

-- If you need to add additional LSP servers or custom configurations,
-- edit the handlers section in your init.lua file instead of this file.

return {}
